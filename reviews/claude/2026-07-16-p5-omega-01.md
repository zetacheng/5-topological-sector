---
This file is a PI-supplied faithful transcript of the independent review performed by Claude on 2026-07-16. Codex is acting only as repository scribe and does not claim authorship of the review.
---

# P5-OMEGA-01 independent review

Gate: P5-OMEGA-01 — omega-stabilised continuum topological object.

Verdict under review, quoted verbatim from `results/topo_verdict.txt`:

> MASS/RADIUS GATE FAILS: MINIMUM IS A CUTOFF-SCALE LATTICE LUMP.

Method: independent re-run of the branch’s own `scripts/topological_mass_radius.py` in a clean Linux environment (Python 3.12.3, NumPy 2.4.4), together with an analytic reconstruction of the `R -> 0` asymptotics from the script’s definitions.

The reviewer’s earlier suspicion that the collapse might be a numerical minimizer or solver artifact was tested and is retracted: no numerical minimizer participates in the verdict.

## Findings

1. **Monotone collapse reproduced.** At `m = 0.30`, `g0 = 20`, with the full nonlocal omega kernel and the eft regulator: `E_tot = +2.544` at `R = 4`; `+0.032` at `R = 0.222`; and `-2.109` at `R = 0.02`. The energy decreases toward the smallest tested radius. The branch’s own `scan_R` returns `interior = False`, `collapse = True`, for every scanned `g0` in `{5, 20, 100, 400}`.
2. **The omega repulsion saturates as `R -> 0`.** Reproduced values are `E_omega = 0.0642` at `R = 1` and `0.0913` at `R = 0.01`. `Btilde(q)=(1+qR)e^(-qR) -> 1` pointwise, while the momentum integral is restricted to `q < Lambda`. A local `1/R^3` contact repulsion would require integration beyond the EFT cutoff. For finite `g0`, `E_omega <= (c_GW^2/2)(1/(2 pi^2))(g0 Lambda^3/3)`.
3. **The omega repulsion saturates as `g0` becomes large.** At `R = 0.02`, `E_omega` is `0.008, 0.091, 0.220, 0.260, 0.268` for `g0 = 1, 20, 400, 10^4, 10^6`; at the same radius `E_4=-2.21`. Wherever `Pi_V(q)>0`, `D00(q)=g0/(1+g0 Pi_V(q)) -> 1/Pi_V(q)`.
4. **The negative verdict is structural within the implemented gate.** With the implemented profile ansatz, finite EFT momentum band, negative verified quartic coefficient, and endogenous nonlocal omega kernel, `E_4 ~ -|B|/R`, the nonlocal omega contribution is bounded, and the two-derivative term tends to zero. Thus increasing `g0` cannot create a continuum minimum; no `g0_crit` exists in the implemented ansatz and EFT kernel.
5. **The regulator choice is favourable to repulsion.** The eft regulator enforces `Pi_V >= 0`, preserving a positive screened kernel in the intended EFT band. The raw continuation with negative `Pi_V` outside the controlled band is not used for the physical verdict.

## Scope

Within the tested one-loop EFT band and implemented topological profile family, there is no stable continuum `B=1` soliton generated by the verified negative quartic coefficient plus endogenous nonlocal omega repulsion. The energy runs toward `R ~ 1/Lambda`, where the continuum EFT is unreliable: a cutoff-scale lattice lump.

The gate does not establish that the complete cutoff-scale lattice theory has no baryonic object, and does not exclude unrelated topological-stabilization mechanisms.

## Disposition

Verdict accepted.

`P5-OMEGA-01 = FAIL` at this order and within the stated scope. The precisely scoped continuum-soliton claim associated with P5-CL-008 must move to `FAILED`.
