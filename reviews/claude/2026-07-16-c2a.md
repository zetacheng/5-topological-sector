# Independent C2a Review — 2026-07-16

## Reviewer

Claude, acting in the independent reviewer/discriminator role defined by this repository.

## Gates reviewed

- P5-C2A-01 — full diagram set and chiral completeness
- P5-C2A-02 — production coefficient scan
- P5-C2A-03 — exact closed-form derivation
- P5-C2A-04 — regulator and Pauli–Villars sign confirmation
- P5-GW-01 — Goldstone–Wilczek normalization, for claim-status recommendation only

## Reviewed state

The reviewer audited `sync/kappa-c2a-paper5-migration` against the PI-supplied `paper5_internal_dimension_v0_22.tex`. Corrective dispositions were subsequently implemented on `fix/audit-2026-07-16`; this record does not treat those Codex changes as a second independent review.

## Independent environment

- Clean Linux environment
- Python with SymPy 1.14.0
- Closed-form region: `ball`
- Reproduction command: `python -m scripts.c2a.derive_kappa_closed ball`

## Artifacts examined

- `scripts/c2a/derive_kappa_closed.py`
- `tests/c2a/test_c2a_regressions.py`
- `results/c2a/raw/closed_form_output.txt`
- `results/c2a/processed/RESULTS_C2a.md`
- `results/c2a/processed/TaskB_PV_signcheck.md`
- `results/c2a/verdict.md`
- `results/goldstone-wilczek/`
- `CLAIMS.md`, `GATES.md`, `MIGRATION.md`, and the supplied v0.22 paper source

## Independent reproduction

The clean-environment closed-form run obtained the numerator

`-90*m**6 - 170*m**4 - 85*m**2 - 17`

and the controlled limit

`kappa_U(m->0) = -17/(1152*pi^2) = -0.00149519`.

All 14 tests present on the reviewed migration branch passed. The Pauli–Villars and Monte Carlo evaluations agree with the exact result to 0.3%. The numerical value, sign, grid, constants, and stored scientific outputs were not challenged by this review.

## Scientific verdict

The C2a scientific reproduction passes: the closed form, negative sign, pointwise cancellation, production evaluation, and regulator checks agree. The audit identified provenance, scope, and robustness defects rather than a numerical defect.

## Defects and dispositions

1. **Cross-paper verdict ownership was contradictory.** `RESULTS_C2a.md` asserted a Paper 4 termination verdict while `verdict.md` disclaimed it. Disposition: this repository now owns the measurement and arithmetic only; the companion paper owns its consequent verdict. Implemented in `f9bd10a`.
2. **The closed-form default was not reproducible.** The undocumented `R4` default failed on divergent monomials although the deliverable used `ball`. Disposition: `ball` is now the default, `R4` remains explicit, divergence raises a descriptive `ValueError`, and the reproduction command is recorded. Implemented in `c87ab44`.
3. **The regression suite did not exercise the symbolic derivation.** The exact answer was encoded only as a hard-coded formula. Disposition: an independent symbolic path now derives the five-class result and is compared with the retained regression anchor. Implemented in `a36bb14`.
4. **The paper source and claim-section links were absent.** Disposition: the PI-supplied v0.22 source is imported unchanged and existing claim rows point to concrete sections. Figures and bibliography remain accurately marked unavailable. Implemented in `20bc549`.
5. **Omega-channel statements appeared in the paper without repository evidence.** Disposition: the four omega health quantities and Fierz pinning are registered as `PROPOSED` under P5-OMEGA-01. The scope-mixed legacy branch was not migrated. Implemented in `e65902c`.
6. **The independent review lacked a durable repository record.** Disposition: this dated review records the environment, reproduction, limitations, defects, and conditional status recommendation. No claim status is changed by this file.

## Claim-status recommendation

Subject to PI acceptance, and conditional on the Task 2 default/error-handling fix and Task 3 symbolic regression test landing:

- P5-CL-001: `SUPPORTED` → `VERIFIED`
- P5-CL-002: `SUPPORTED` → `VERIFIED`
- P5-CL-003: `SUPPORTED` → `VERIFIED`
- P5-CL-007: `SUPPORTED` → `VERIFIED`

The corrective branch contains the requested Task 2 and Task 3 implementations, but the recommendation is not itself PI acceptance. Codex must not apply these promotions without the PI's explicit decision.

Confirmed without status change:

- P5-CL-004 remains `FAILED`.
- P5-CL-005 remains `FAILED`.
- P5-CL-006 remains `RETIRED`.
- P5-CL-008 remains `INCONCLUSIVE`.

## Limitations and pending work

- The reviewer did not independently certify the scope-mixed omega/Fierz artifacts; P5-CL-009 through P5-CL-013 remain `PROPOSED`.
- The nonlocal continuum-radius gate has not been run in this repository.
- Paper figures and bibliography were not supplied.
- PI acceptance is pending, so no claim is promoted and no paper prose is revised by this review record.

## Reviewer status recommendation

Scientific reproduction: **PASS**.  
Claim promotion: **RECOMMENDED CONDITIONALLY FOR PI DECISION**.  
PI acceptance: **PENDING**.

## Acceptance

- Review date: 2026-07-16
- Environment: clean Linux environment
- Bare default invocation: passed
- Explicit `R4` failure: loud and typed `ValueError`
- Mutation test: demonstrated that the symbolic derivation path is genuinely tested
- Tests: 15/15 passed
- Raw scientific artifacts: unchanged
- Reviewer verdict: **PASS**
- PI decision: **ACCEPTED**

Claims promoted:

- P5-CL-001
- P5-CL-002
- P5-CL-003
- P5-CL-007

Claims explicitly not promoted:

- All omega claims remain `PROPOSED` because their authoritative artifacts have not yet been migrated into this repository.
- P5-CL-008 remains `INCONCLUSIVE`.
