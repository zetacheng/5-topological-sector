# Normalization Summary

The legacy loop output gives (c_{GW}(a\to0)=0.999885) with vanishing imaginary part at (m=0.30), (R_{max}=4), and reports a hard-gate tolerance of 0.005 around unity. The cutoff scan approaches unity and the two recorded masses retain the same topological normalization within the source's numerical accuracy.

This summary is downstream of the immutable raw output and does not include the Paper 3 C6 matching.

